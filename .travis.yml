# defaults
language: node_js
node_js: '12'

install: npm ci --ignore-scripts

# keep the npm cache around to speed up installs
cache:
  directories:
    - ~/.npm # cache npm's cache
jobs:
  include:
    - stage: lint
      script: npm run lint
    - stage: publish
      script: npm publish
